# ── Swiss Knife Productivity ─────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
sqlalchemy>=2.0.0
pydantic>=2.0.0

# MCP Server (optional — for Hermes Agent integration)
mcp>=1.0.0,<2                # FastMCP API (v1)